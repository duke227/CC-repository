# CC - Claude Code 工作区

这是跨电脑共享的 Claude Code 项目工作区。

## 项目说明

（在这里添加你的项目描述，Claude Code 会自动读取此文件）

## 常用命令

（在这里添加项目的常用命令，如构建、测试、运行等）

## 注意事项

- 全局 CC 配置在 `shared-claude-config/` 中管理
- 在新电脑上克隆后运行 `sync.ps1`（Windows）或 `sync.sh`（Mac/Linux）
- 在本机创建新 memory/定时任务后，运行 `collect` 脚本同步到仓库
